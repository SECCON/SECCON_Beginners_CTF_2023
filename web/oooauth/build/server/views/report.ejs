<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Report - OAuth Authorization Server</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
</head>

<body>
    <div class="container">
        <h1><a href="/">OAuth Authorization Server</a></h1>
    </div>
    <div class="container">
        <h2>Report</h2>
        <form class="form" action="/report" method="POST">
            <div class="form-group">
                <label for="query">query:</label>
                <input type="text" id="query" name="query" class="form-control" placeholder="query (e.g. ?response_type=code&...&scopes=email profile)">
            </div>
            <p>If you set <code>?response_type=code&...&scopes=email profile</code> as path, admin will check
                <code>https://oooauth.beginners.seccon.games:3001/auth?response_type=code&...&scopes=email profile</code></p>
            <input type="submit" class="btn btn-primary" value="Send" />
        </form>
        <% if (success !== "") { %>
            <p class="text-success"><%= success %></p>
        <% } %>
        <% if (error !== "") { %>
            <p class="text-danger"><%= error %></p>
        <% } %>
    </div>
</body>

</html>